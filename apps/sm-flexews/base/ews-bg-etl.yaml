---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ews-bg-etl
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ews-bg-etl
  template:
    metadata:
      labels:
        app: ews-bg-etl
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: api
        image: nginxinc/nginx-unprivileged:alpine
        ports:
        - containerPort: 8080
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ews-bg-etl
spec:
  type: ClusterIP
  selector:
    app: ews-bg-etl
  ports:
  - port: 80
    targetPort: 8080
