---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ews-web-be-public
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ews-web-be-public
  template:
    metadata:
      labels:
        app: ews-web-be-public
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: api
        image: nginxinc/nginx-unprivileged:alpine
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: "1000m"
            memory: "1500Mi"
          limits:
            cpu: "2000m"
            memory: "3000Mi"
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /api/global
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 3 
        env:
        - name: TZ
          value: "UTC"          
        envFrom:
        - configMapRef:
            name: ews-web-be-public-configmap
---
apiVersion: v1
kind: Service
metadata:
  name: ews-web-be-public
spec:
  type: ClusterIP
  selector:
    app: ews-web-be-public
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ews-web-be-public-configmap
data:
  LOG_LEVEL: "info"
