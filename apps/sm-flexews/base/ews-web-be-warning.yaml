---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ews-web-be-warning
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ews-web-be-warning
  template:
    metadata:
      labels:
        app: ews-web-be-warning
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: api
        image: nginxinc/nginx-unprivileged:alpine
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: "1500m"
            memory: "2000Mi"
          limits:
            cpu: "2500m"
            memory: "4000Mi"
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /api/global
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 3 
        env:
        - name: TZ
          value: "UTC"          
        envFrom:
        - configMapRef:
            name: ews-web-be-warning-configmap
---
apiVersion: v1
kind: Service
metadata:
  name: ews-web-be-warning
spec:
  type: ClusterIP
  selector:
    app: ews-web-be-warning
  ports:
  - port: 80
    targetPort: 8080
